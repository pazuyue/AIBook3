# /idea-boost

Description: 按 Trae 剧情策划 skill 执行 `/idea-boost`。

## Source of truth
- 读取并服从 `.trae\rules\三月女神剧情策划skill.md`。

## Steps
1. 只输出创意机会，不写正文，不直接修改文件。
2. 必须服从 `设定\当前硬口径.md` 和当前卷边界。
3. 围绕用户指定章节、桥段或问题给出可落地的剧情增强点。
4. 不新增会破坏主线、战力体系、隐藏身份或秘史揭露梯度的设定。
5. 输出保持短、具体、可执行。
